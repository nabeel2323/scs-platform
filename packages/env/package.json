{
  "name": "@scs/env",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Commerce — Zod-validated environment schemas per app",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.5.4",
    "rimraf": "^6.0.1"
  }
}
