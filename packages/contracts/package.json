{
  "name": "@scs/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Commerce — Shared API contracts (zod schemas → OpenAPI 3.1 → generated clients)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "tsx scripts/generate-openapi.ts",
    "lint": "eslint src/ --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.2"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "rimraf": "^6.0.1"
  }
}
